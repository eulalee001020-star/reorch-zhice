{
  "tenant_id": "CUSTOMER_TENANT",
  "contract_id": "machine-down-recovery",
  "scenario_type": "machine_down_recovery",
  "version": "1.0.0",
  "description": "Minimum decision facts for one machine-down recovery scenario.",
  "field_requirements": [
    {
      "canonical_path": "operation.operation_id",
      "authority_role": "operation_authority",
      "data_type": "string",
      "criticality": "hard",
      "nullable": false,
      "minimum_coverage": 1.0,
      "max_age_seconds": 300,
      "quality_rules": [{"rule_type": "unique", "parameters": {}}]
    },
    {
      "canonical_path": "operation.status",
      "authority_role": "operation_authority",
      "data_type": "string",
      "criticality": "hard",
      "nullable": false,
      "minimum_coverage": 1.0,
      "max_age_seconds": 60,
      "quality_rules": [
        {
          "rule_type": "allowed_values",
          "parameters": {"values": ["planned", "released", "started", "completed", "blocked"]}
        }
      ]
    },
    {
      "canonical_path": "operation.planned_end",
      "authority_role": "operation_authority",
      "data_type": "datetime",
      "criticality": "hard",
      "nullable": false,
      "minimum_coverage": 1.0,
      "max_age_seconds": 300,
      "quality_rules": [{"rule_type": "timezone_aware", "parameters": {}}]
    }
  ],
  "constraint_requirements": [
    {
      "constraint_type": "resource_eligibility",
      "authority_role": "operation_authority",
      "criticality": "hard",
      "minimum_coverage": 1.0
    }
  ],
  "evidence_refs": ["customer:scenario-owner-signoff:REF"]
}
